public interface IQuadroGeral {
	public String[][] porcentagem(String matriz[][]); /* Retorna uma matriz que associa cada doença com a porcentagem de pacientes que a possuem */
	public void plotarGrafico(String matriz[][]);
}
